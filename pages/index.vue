<template><Button></Button></template>

<script setup lang="ts">
// const mongoose = require("mongoose");

// const username: string = process.env.MONGODB_USERNAME || "";
// const password: string = process.env.MONGODB_PASSWORD || "";
// // const uri = `mongodb://${username}:${password}@playgroundpal.hxpzfdx.mongodb.net/?retryWrites=true&w=majority&appName=playgroundpal`;

// const uri =
//   "mongodb://dev:devpw@playgroundpal.hxpzfdx.mongodb.net/?retryWrites=true&w=majority&appName=playgroundpal";

// mongoose.connect(uri).catch((err) => console.log(err));

// const clientOptions = {
//   serverApi: { version: "1", strict: true, deprecationErrors: true },
// };

// async function run() {
//   try {
//     // Create a Mongoose client with a MongoClientOptions object to set the Stable API version
//     await mongoose.connect(uri, clientOptions);
//     await mongoose.connection.db.admin().command({ ping: 1 });
//     console.log(
//       "Pinged your deployment. You successfully connected to MongoDB!",
//     );
//   } finally {
//     // Ensures that the client will close when you finish/error
//     await mongoose.disconnect();
//   }
// }
// run().catch(console.dir);
</script>
